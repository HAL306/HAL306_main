using UnityEngine;


/// <summary>
/// 地形を別オブジェクトに接続するコンポーネント
/// </summary>
public class TerrainConect : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
